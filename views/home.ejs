<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Ruby on rails includes head.ejs in home page -->
    <% include partials/head.ejs %>

  </head>
  <body>
    
    <% for (var i =0; i < photos.length; i++) { %>
      <!-- This will get the images from the class called poster in images. -->
      <img src="/images/<%= photos[i].poster %>" class="img-responsive">
    <% } %>
    <!-- stopped at loop through photos -->


    <!-- Ruby on rails includes script.ejs in home page -->
    <% include partials/script.ejs %>
  </body>
</html>